import SwiftUI

struct SettingsScreen: View {
    var body: some View {
        ZStack {
            Color.whiteDayNight
                .ignoresSafeArea()
            Text("Here will be the settings screen")
        }
    }
}

#Preview {
    SettingsScreen()
}
